import { ElementType } from "react";

export type PlatformDataType = {
  name: string;
  Icon: ElementType;
};

export type platformLinkType = {
  name: string;
  url: string;
  Icon: ElementType;
};
